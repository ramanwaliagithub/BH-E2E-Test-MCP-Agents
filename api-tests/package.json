{
  "name": "bighappy-api-tests",
  "version": "1.0.0",
  "description": "API test suite for ReqRes API using Node.js and Jest",
  "main": "index.js",
  "scripts": {
    "test": "jest --verbose",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage",
    "test:ci": "jest --ci --coverage --maxWorkers=2"
  },
  "keywords": [
    "api-testing",
    "jest",
    "reqres"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.6.2"
  },
  "devDependencies": {
    "allure-jest": "^3.10.2",
    "jest": "^29.7.0"
  }
}
